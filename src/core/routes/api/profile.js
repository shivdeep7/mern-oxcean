/**
 * 
 *  Route to Setup the user profile 
 * 
 */
const route = require("express").Router();
const { check, validationResult } = require("express-validator");

// Get the middlewares


/**
 * 
 *  @Route    /api/profile
 *  @Desc     Route to set the basic user profile options
 *  @Access   Private 
 *  @User     Admin
 *
 **/
router.get([auth], ( req, res ) => {

    // Get the user data 


})


 module.exports = route;
